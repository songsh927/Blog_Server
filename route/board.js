import express, { Router } from "express";
import 'exress-async-errors';

const router = express.Router();

//getAllArticle (menuid)

//getMoreArticle (menuid)

//getArticle (menuid, articleid)

//postArticle (title, text, uploadTime)

//updateArticle (articleid, title, text, uploadTime)

//deleteArticle (articleid)



export default Router;